apply plugin: 'com.android.library'
apply plugin: 'expo-module-gradle-plugin'

group = 'expo.modules.mobilydevicekey'
version = '1.0.0'

android {
  namespace 'expo.modules.mobilydevicekey'

  defaultConfig {
    versionCode 1
    versionName '1.0.0'
  }
}

dependencies {
  implementation('androidx.biometric:biometric:1.1.0')
}
